FROM nginx:alpine

# Change the NGINX config to listen on 8080
RUN sed -i 's/listen\s\+80;/listen 8888;/' /etc/nginx/conf.d/default.conf

WORKDIR /usr/share/nginx/html
COPY ./index.html .
COPY Image.png .
RUN chmod -R 755 /usr/share/nginx/html

EXPOSE 8080
